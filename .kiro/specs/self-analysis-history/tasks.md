# Implementation Plan

## 1. Foundation: テスト基盤・依存・スキーマ移行

- [x] 1.1 vitest をテスト基盤として最小導入する
  - candidate アプリにテスト実行環境（vitest devDependency・設定・`test` スクリプト）を用意し、純関数ディレクトリ配下の `*.test.ts` のみを対象にする
  - monorepo のタスクパイプラインに `test` を追加し、ルートからテストを起動できるようにする
  - 観測可能な完了条件: 空のサンプルテスト1件が `pnpm test`（candidate スコープ）で緑になり、CI 相当のローカル実行が通る
  - _Requirements: 4.1, 5.1, 2.1_

- [x] 1.2 成長推移グラフ用に recharts を導入する
  - candidate アプリに recharts を追加し、React 19 互換のため `react-is` を React 19 系へ固定（pnpm overrides）する
  - 観測可能な完了条件: recharts を import した最小コンポーネントがビルド（`pnpm build` / typecheck）を通過し、peer 解決エラーが出ない
  - _Requirements: 4.1_
  - _Depends: 1.1_

- [x] 1.3 回答・自己分析を追記型化するスキーマ移行を行う
  - 回答テーブルの (候補者, アンケート) 一意制約を撤廃し、最新/履歴取得用の複合インデックス（候補者・アンケート・提出日時）を追加する
  - 自己分析テーブルの一意キーを (候補者, アンケート) から版キー（source_response_id）へ付け替え、最新/履歴取得用インデックスを追加する
  - マイグレーションを生成・ローカル適用し、既存データが版1として無損失で残ることを確認する
  - 観測可能な完了条件: マイグレーション適用後、回答・自己分析の行数が不変で、自己分析の版キーが一意であることをローカル Postgres で確認できる
  - _Requirements: 7.1_

## 2. Core: データアクセス層の版スコープ化

- [x] 2.1 (P) 自己分析クエリを版スコープへ刷新する
  - 最新版取得（最新提出日時の自己分析を返す）・版履歴取得（昇順の版配列）・指定版取得（版キー指定）を提供する
  - 再生成抑制カウンタの判定を版キー単位へ変更し、上書き保存の競合キーを版キーへ揃える
  - すべての読み出しを本人の候補者プロフィールに限定する
  - 観測可能な完了条件: 同一候補者・同一アンケートに複数版が存在する状態で、最新版取得が最新1件、履歴取得が全版を昇順で返す
  - _Requirements: 3.1, 3.2, 3.3, 3.5, 4.1, 5.1, 6.1, 6.3_
  - _Boundary: self-analysis-query_
  - _Depends: 1.3_

- [x] 2.2 (P) 回答ソース取得を版指定・最新解決に分離し、クールダウン判定用クエリを追加する
  - 自己分析生成用の回答取得を「最新回答の解決」と「版（回答ID）指定取得」に分け、一意制約への暗黙依存を排除する
  - クールダウン判定のため (候補者, アンケート) の最新提出日時を返すクエリを追加する
  - すべての読み出しを本人に限定し、既存の戻り型（回答束の形）は維持する
  - 観測可能な完了条件: 複数版が存在しても最新解決は最新回答を、版指定取得は指定版の回答束を返す
  - _Requirements: 1.1, 2.1, 3.1, 6.1, 6.3_
  - _Boundary: analysis-source-query_
  - _Depends: 1.3_

- [ ] 2.3 (P) スキル棚卸し最新回答クエリを最新版保証へ修正し、全消費箇所を棚卸しする
  - 結果ページ等が使う最新回答取得を、提出日時の降順で最新1件を返すよう明示修正する（一意制約前提の暗黙依存を除去）
  - 両テーブルの全消費箇所を横断検索し、一意制約・最新1件前提の残存箇所が無いことを確認する
  - 観測可能な完了条件: 複数版が存在しても結果ページ向けクエリが最新回答を返し、棚卸しで未対応の消費箇所が0件であることを確認できる
  - _Requirements: 7.2, 1.2_
  - _Boundary: skill-survey-query_
  - _Depends: 1.3_

- [ ] 2.4 新規・改修クエリを公開エクスポートに反映する
  - 履歴・指定版・最新提出日時の各クエリを利用側から参照できるよう公開する
  - 観測可能な完了条件: アプリ層から新規クエリを import してビルド（typecheck）が通る
  - _Requirements: 4.1, 5.1, 2.1_
  - _Depends: 2.1, 2.2_

## 3. Core: 純関数（単体テスト付き）

- [ ] 3.1 (P) 再回答クールダウン判定の純関数を実装しテストする
  - 最新提出日時と現在時刻から、同一アンケートの再回答可否と再開日時を算出する（既定30日、現在時刻は引数注入で決定論化）
  - 初回（履歴なし）は許可、29日経過は拒否＋再開日、30日ちょうどは許可、の境界を単体テストで固定する
  - 観測可能な完了条件: 境界3ケースを含む単体テストが緑になる
  - _Requirements: 2.1, 2.2, 2.3, 2.4_
  - _Boundary: _lib/cooldown_
  - _Depends: 1.1_

- [ ] 3.2 (P) 成長推移の時系列整形の純関数を実装しテストする
  - 版履歴から全体網羅度とカテゴリ別網羅度の時系列を生成する。サマリ失敗（可視化のみ）の版も網羅度は系列に含める
  - 0件・1件・複数件、可視化のみ版を含むケースを単体テストで固定する
  - 観測可能な完了条件: 1件入力で単点、可視化のみ版が網羅度系列に含まれることをテストで確認できる
  - _Requirements: 4.1, 4.2, 4.3, 4.4_
  - _Boundary: _lib/trend_
  - _Depends: 1.1, 2.1_

- [ ] 3.3 (P) 2版差分計算の純関数を実装しテストする
  - 2つの版から全体・カテゴリ別の網羅度増減差分を算出する（新規・消失カテゴリも扱う）
  - カテゴリ増減・新規/消失の各ケースを単体テストで固定する
  - 観測可能な完了条件: 代表ケースで期待差分を返す単体テストが緑になる
  - _Requirements: 5.1, 5.3_
  - _Boundary: _lib/compare_
  - _Depends: 1.1, 2.1_

## 4. Core: Server Actions の版対応

- [ ] 4.1 回答提出を追記型化し30日クールダウンを適用する
  - 回答提出を上書き（upsert＋既存回答削除）から新規版の追記へ変更し、過去版とその回答を保持する
  - 提出前に最新提出日時とクールダウン判定を行い、期間内は再開日付きの拒否を返す（最終防衛線）。初回は対象外
  - 観測可能な完了条件: 30日以内の再提出が拒否され、期間経過後は新しい版が追加され過去版の回答が残ることをローカルで確認できる
  - _Requirements: 1.1, 1.2, 1.3, 2.1, 2.2, 2.3, 2.4_
  - _Depends: 2.2, 3.1_

- [ ] 4.2 自己分析の生成・再生成を版スコープ化する
  - 生成は最新回答版を対象に版キー単位で保存し、再生成は対象版の回答を入力に自然言語のみ更新（集計は不変）する
  - 再生成抑制カウンタを版キー単位で判定・反映し、30日クールダウンとは独立した日次上限として機能させる
  - 観測可能な完了条件: 同一版へのサマリ再生成で版数が増えず行が更新され、新しい回答版に対しては新規の自己分析行が追加される
  - _Requirements: 3.1, 3.2, 3.5_
  - _Depends: 2.1, 2.2_

## 5. Core: 履歴・比較 UI

- [ ] 5.1 (P) 強み/弱み/成長アクション表示を共有コンポーネントへ抽出する
  - 現在最新版表示内にあるナラティブ表示を独立した共有コンポーネントとして切り出し公開する
  - 最新版表示を抽出後の共有コンポーネント利用へ置き換え、表示が従来どおりであることを保つ
  - 観測可能な完了条件: 最新版表示が見た目・内容とも従来どおりで、抽出コンポーネントを比較UIから再利用できる状態になる
  - _Requirements: 5.2_
  - _Boundary: narrative-section, self-analysis-view_

- [ ] 5.2 (P) 成長推移グラフを実装する
  - 時系列整形の出力を受け取り、全体網羅度の折れ線を既定表示し、カテゴリ別を凡例トグルで重ねる。可視化のみ版も網羅度を表示する
  - グラフはクライアント側に隔離し、SSR を避ける形で読み込む。数値序列・他者比較は表示しない
  - 観測可能な完了条件: 2件以上の版で推移グラフが描画され、カテゴリ系列の表示切替が動作する
  - _Requirements: 4.1, 4.2, 4.3, 4.5_
  - _Boundary: coverage-trend-chart_
  - _Depends: 1.2, 3.2_

- [ ] 5.3 2版比較ビューを実装する
  - 履歴から2版を選択し、各版の網羅度を左右並置し差分を示す。両版にサマリがあれば強み・成長アクションを新旧対比、片方でも無ければ網羅度差分のみ表示する
  - 観測可能な完了条件: 2版選択で差分と新旧対比が表示され、可視化のみ版を含む選択では差分のみ表示になる
  - _Requirements: 5.1, 5.2, 5.3_
  - _Depends: 3.3, 5.1_

- [ ] 5.4 履歴セクションを統括するコンテナを実装する
  - サーバから渡る版履歴を保持し、推移グラフと2版比較を束ねる。版が1件のときは単点表示・比較無効、0件のときは履歴非表示にする
  - 観測可能な完了条件: 版1件で比較UIが出ず単点表示、2件以上で推移と比較の双方が表示される
  - _Requirements: 4.4, 5.4_
  - _Depends: 5.2, 5.3_

## 6. Integration: ページ配線とクールダウン入口抑止

- [ ] 6.1 自己分析ページに履歴セクションを配線する
  - ページで版履歴を追加取得し、最新版表示の下に履歴セクションを内包する。本人のみ・未認証はサインイン誘導の既存ガードを維持する
  - 最新回答版に自己分析が未生成の場合は最新版の生成を促しつつ、過去版の履歴と推移を閲覧可能にする（陳腐化導線の維持）
  - 観測可能な完了条件: 再回答直後（最新版未生成）でも履歴・推移が閲覧でき、最新版生成導線が表示される
  - _Requirements: 3.3, 3.4, 4.1, 5.4, 6.1, 6.2_
  - _Depends: 2.1, 4.2, 5.4_

- [ ] 6.2 再回答入口にクールダウン状態を反映する
  - 結果ページの「回答を編集する」導線と回答フォーム入口で、クールダウン中は再回答を抑止し再開日を表示する（提出時拒否と同一判定を共有）
  - 観測可能な完了条件: クールダウン中は入口で再開日が表示され再回答に進めず、期間経過後は通常どおり進める
  - _Requirements: 2.1, 2.2_
  - _Depends: 2.2, 3.1_

## 7. Validation: 統合・受入・回帰確認

- [ ] 7.1 データ層の統合動作をローカルで検証する
  - 追記保持・30日クールダウン・版スコープ生成/再生成・最新版/履歴取得を、ローカル Postgres 実データで一通り確認する
  - 観測可能な完了条件: 再回答で行が増え過去回答が残る／クールダウン拒否と経過後受理／同一版再生成で版数不変／最新版・履歴の各クエリ結果が期待どおり、を確認できる
  - _Requirements: 1.1, 1.2, 2.1, 2.2, 3.1, 3.2, 3.3, 3.5, 6.3_
  - _Depends: 4.1, 4.2_

- [ ] 7.2 履歴・比較UIの受入を確認する
  - 推移グラフ表示（2件以上）、1件時の単点・比較無効、2版比較の差分・新旧対比、可視化のみ版を含む比較の縮退表示、序列非表示を手動確認する
  - 観測可能な完了条件: 受入観点の各ケースが画面上で期待どおりに表示される
  - _Requirements: 4.1, 4.4, 4.5, 5.1, 5.3, 5.4_
  - _Depends: 6.1_

- [ ] 7.3 既存挙動の非破壊回帰を確認する
  - 結果ページが最新回答を表示すること、自己分析の生成・再生成・最新版表示・陳腐化導線が従来どおり動くこと、横断比較が提供されないことを確認し、typecheck/build を通す
  - 観測可能な完了条件: 既存フローが従来どおり動作し、typecheck と build が緑になる
  - _Requirements: 7.2, 7.3, 3.3_
  - _Depends: 6.1, 6.2_

## Implementation Notes

- 1.3: ローカル Docker Postgres は過去に `drizzle-kit push` で構築されており `__drizzle_migrations` 追跡テーブルが空。そのため `drizzle-kit migrate` は全マイグレーションを再適用しようとしてハングする。ローカル検証では生成済み migration SQL（0014）を `docker exec ... psql` で直接適用した。migration ファイル自体は本番（Neon）向けに正しく生成済み。以降の DB タスクでローカル適用が必要な場合は、生成 SQL を psql 直接適用するか、対象 DDL のみを手動適用すること。本番適用は別途運用で実施。

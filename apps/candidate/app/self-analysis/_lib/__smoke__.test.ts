/**
 * Smoke test — proves the vitest harness is wired correctly.
 * This file is a placeholder; delete or replace when real _lib tests arrive.
 */
import { describe, it, expect } from "vitest";

describe("vitest harness smoke test", () => {
  it("runs a trivial assertion", () => {
    expect(1 + 1).toBe(2);
  });
});
